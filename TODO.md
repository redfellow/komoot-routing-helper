## TODO

1: [x] Add a disable/enable switch toggle to the floating popup, so a user can quickly toggle the visual changes the extension makes.

2: Make the visual changes to trails actually work, I get that we made the MapLibre layer based on how Squadrats plugin has handled things, but Im not seeing any visaul changes on the map when changing trail settings via the extension. Keep testing with Brave until you can verify that changing the trail options works on the fly
